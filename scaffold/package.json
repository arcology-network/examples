{
  "name": "scaffold",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test:serial": "for f in test/*.ts; do npx hardhat run $f --network TestnetInfo 2>&1 | grep -v 'Nothing to compile' || exit 1; done",
    "benchmark:boolArray": "HARDHAT_NETWORK=TestnetInfo  npx hardhat run benchmark/boolArray/gen-tx-bookArray.ts",
    "benchmark:like": "HARDHAT_NETWORK=TestnetInfo  npx hardhat run benchmark/like/gen-tx-like.ts",
    "benchmark:parallelSubCurrency": "HARDHAT_NETWORK=TestnetInfo  npx hardhat run benchmark/parallelSubCurrency/gen-tx-parallelSubCurrency.ts",
    "benchmark:subCurrency": "HARDHAT_NETWORK=TestnetInfo  npx hardhat run benchmark/subCurrency/gen-tx-subCurrency.ts"
  },
  "devDependencies": {
    "@arcologynetwork/concurrentlib": "^2.2.0",
    "@arcologynetwork/frontend-tools": "^1.0.12",
    "@arcologynetwork/frontend-util": "^1.0.11",
    "@nomicfoundation/hardhat-ethers": "^4.0.3",
    "@nomicfoundation/hardhat-ignition": "^3.0.3",
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.0",
    "@types/chai": "^4.3.20",
    "@types/chai-as-promised": "^8.0.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.18.11",
    "@types/progress": "^2.0.7",
    "chai": "^5.3.3",
    "ethers": "^6.15.0",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.0.7",
    "mocha": "^11.7.4",
    "typescript": "~5.8.0"
  },
  "dependencies": {
    "progress": "^2.0.3"
  }
}
